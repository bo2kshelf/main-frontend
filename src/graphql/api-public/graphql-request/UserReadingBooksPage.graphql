query AllUserReadingBooksPage {
  allUsers {
    userName: uniqueName
    books: readingBooks {
      count
    }
  }
}

query UserReadingBooksPage($userName: String!, $skip: Int!, $limit: Int!) {
  user(uniqueName: $userName) {
    userName: uniqueName
    displayName
    picture
    books: readingBooks(limit: $limit, skip: $skip) {
      count
      hasNext
      hasPrevious
      nodes {
        book {
          id
          title
          cover
        }
      }
    }
  }
}
