query AllUserWishReadBooksPage {
  allUsers {
    userName: uniqueName
    books: wishesReadBooks {
      count
    }
  }
}

query UserWishReadBooksPage($userName: String!, $skip: Int!, $limit: Int!) {
  user(uniqueName: $userName) {
    userName: uniqueName
    displayName
    picture
    books: wishesReadBooks(limit: $limit, skip: $skip) {
      count
      hasNext
      hasPrevious
      nodes {
        book {
          id
          title
          cover
        }
      }
    }
  }
}
