query AllUserWishReadBooksPage {
  allUsers {
    userName: uniqueName
    books: wishBooks {
      count
    }
  }
}

query UserWishReadBooksPage($userName: String!, $skip: Int!, $limit: Int!) {
  user(uniqueName: $userName) {
    userName: uniqueName
    displayName
    picture
    records {
      count
    }
    readBooks {
      count
    }
    readingBooks {
      count
    }
    wishBooks(limit: $limit, skip: $skip) {
      count
      hasNext
      hasPrevious
      nodes {
        book {
          id
          title
          cover
        }
      }
    }
    haveBooks {
      count
    }
    stackedBooks {
      count
    }
  }
}
