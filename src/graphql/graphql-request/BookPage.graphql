query AllBookPages {
  allBooks {
    id
  }
}

query BookPage($id: ID!) {
  book(id: $id) {
    id
    title
    subtitle
    isbn(dehyphenize: true)
    cover
    publishedBy {
      id
      name
      publications(limit: 12, except: [$id]) {
        nodes {
          book {
            id
            title
            cover
          }
        }
      }
    }
    writedBy {
      author {
        id
        name
        writed(limit: 12, except: [$id]) {
          nodes {
            book {
              id
              title
              cover
            }
          }
        }
      }
      roles
    }
    seriesOf {
      series {
        id
        title
        parts(limit: 24, orderBy: ASC) {
          nodes {
            book {
              id
              title
              cover
            }
          }
        }
      }
    }
  }
}
