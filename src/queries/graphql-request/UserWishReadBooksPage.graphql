query AllUserWishReadBooksPage {
  allUsers {
    uniqueName
    books: wishReadBooks {
      count
    }
  }
}

query UserWishReadBooksPage($uniqueName: String!, $skip: Int!, $limit: Int!) {
  user(uniqueName: $uniqueName) {
    uniqueName
    displayName
    picture
    books: wishReadBooks(limit: $limit, skip: $skip) {
      count
      skip
      limit
      hasNext
      hasPrevious
      records {
        book {
          id
          title
          cover
        }
      }
    }
  }
}
