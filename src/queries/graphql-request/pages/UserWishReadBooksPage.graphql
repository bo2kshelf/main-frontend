query AllUserWishReadBooksPage {
  allAccounts {
    userName
    wishReadBooks {
      count
    }
  }
}

query UserWishReadBooksPage($userName: ID!, $skip: Int!, $limit: Int!) {
  account(userName: $userName) {
    userName
    displayName
    picture
    wishReadBooks(limit: $limit, skip: $skip) {
      count
      skip
      limit
      hasNext
      hasPrevious
      records {
        book {
          id
          title
          cover
        }
      }
    }
  }
}
