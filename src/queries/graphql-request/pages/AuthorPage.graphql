query AllAuthorPages {
  allAuthors {
    id
  }
}

query AuthorPage($id: ID!) {
  author(id: $id) {
    id
    name
    writes(limit: 20) {
      book {
        id
        title
        isbn
        cover
      }
    }
  }
}
