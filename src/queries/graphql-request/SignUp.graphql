mutation SignUp(
  $userId: ID!
  $userName: ID!
  $displayName: String!
  $picture: String!
) {
  createAccount(
    data: {
      userId: $userId
      userName: $userName
      displayName: $displayName
      picture: $picture
    }
  ) {
    id
    userName
    displayName
    picture
  }
}
